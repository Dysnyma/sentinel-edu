
# 

# 问题
1. 导入音频文件后,你切换模型,他就会显示"转写结果（可手动编辑）",然后底下是一个空窗口,如果你不刷新,你即使再次提交文件,他都只会显示空窗口.

2. 导入新文件后，然后我再切模型，它会出现问题1差不多的结果，就是显示的不是空窗口，而是上一次的结果


3. 
```
步骤4：投毒生成有毒文本（红队测试）
投毒比例


0.10

1.00


投毒生成中 27/27 | 耗时 38s | 预计剩余 0s

第11条投毒失败

查看错误

AttributeError: 'str' object has no attribute 'setdefault'
Traceback:
File "/home/ckc/projects/sentinel-edu/views/helpers.py", line 137, in run_concurrently
    results[idx] = future.result()
                   ^^^^^^^^^^^^^^^
File "/usr/lib/python3.12/concurrent/futures/_base.py", line 449, in result
    return self.__get_result()
           ^^^^^^^^^^^^^^^^^^^
File "/usr/lib/python3.12/concurrent/futures/_base.py", line 401, in __get_result
    raise self._exception
File "/usr/lib/python3.12/concurrent/futures/thread.py", line 58, in run
    result = self.fn(*self.args, **self.kwargs)
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
File "/home/ckc/projects/sentinel-edu/core/poison_generator.py", line 107, in generate_poison
    result.setdefault('original_text', text)
    ^^^^^^^^^^^^^^^^^
Copy
Ask Google
Ask ChatGPT
```

4. 有毒样本对比不应该放在最右边，只有右边一点点空间，应该放在下面
# 功能添加
- 文本纠错后的对比功能，双栏对比，文本高亮，这个时候就已经可以提前分为多个josnl了
- 调用api给数据集命名的功能，不然都不知道我们保存的数据集是什么（包括原始数据集（转文字后的直接结果），文本纠错后的数据集其实也就是生成无毒文本集，投毒生成有毒文本的数据集，这三个数据集都要命名好）